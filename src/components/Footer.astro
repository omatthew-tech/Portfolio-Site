---
import { site } from "@/lib/site";
---

<footer class="site-footer">
  <div class="site-container footer-inner">
    <div>
      <p class="footer-copyright">&copy; 2026 Matthew Ozoroski. All rights reserved</p>
    </div>
    <nav aria-label="Footer links">
      <a href={`mailto:${site.email}`}>Email</a>
      <a href={site.socials.linkedin}>LinkedIn</a>
    </nav>
  </div>
</footer>

<style>
  .site-footer {
    border-block-start: 1px solid var(--color-surface-muted);
    margin-block-start: var(--gap-section);
    padding-block: 2rem;
  }

  .footer-inner {
    align-items: center;
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-200) var(--spacing-400);
    justify-content: space-between;
  }

  .footer-copyright {
    color: var(--color-muted);
    margin: 0;
  }

  .site-footer nav {
    align-items: center;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 1rem;
  }

  .site-footer nav a {
    align-items: center;
    display: inline-flex;
    min-height: 44px;
  }
</style>
